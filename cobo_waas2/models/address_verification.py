# coding: utf-8

"""
    Cobo Wallet as a Service 2.0

    Contact: help@cobo.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from cobo_waas2.models.address_verification_method import AddressVerificationMethod
from cobo_waas2.models.address_verification_status import AddressVerificationStatus
from typing import Optional, Set
from typing_extensions import Self


class AddressVerification(BaseModel):
    """
    Summary of a self-custody address verification record. Returned as items in [List address verifications](#operation/list_address_verifications). Call [Get address verification](#operation/get_address_verification) with `verification_id` to retrieve method-specific provenance (`satoshi_test_detail` / `signature_detail`). 
    """  # noqa: E501
    verification_id: StrictStr = Field(description="The unique identifier of this verification record.")
    address: StrictStr = Field(description="The counterparty (self-custody) wallet address being verified.")
    chain_id: StrictStr = Field(description="The chain on which this address is verified.")
    status: AddressVerificationStatus
    verification_method: AddressVerificationMethod
    verified_at: Optional[StrictInt] = Field(default=None, description="Timestamp (milliseconds) when verification completed. `null` while `status=PENDING` or `FAILED`.")
    __properties: ClassVar[List[str]] = ["verification_id", "address", "chain_id", "status", "verification_method", "verified_at"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AddressVerification from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if verified_at (nullable) is None
        # and model_fields_set contains the field
        if self.verified_at is None and "verified_at" in self.model_fields_set:
            _dict['verified_at'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AddressVerification from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "verification_id": obj.get("verification_id"),
            "address": obj.get("address"),
            "chain_id": obj.get("chain_id"),
            "status": obj.get("status"),
            "verification_method": obj.get("verification_method"),
            "verified_at": obj.get("verified_at")
        })
        return _obj


