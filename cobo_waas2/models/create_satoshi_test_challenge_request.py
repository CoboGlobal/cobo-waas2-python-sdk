# coding: utf-8

"""
    Cobo Wallet as a Service 2.0

    Contact: help@cobo.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from cobo_waas2.models.satoshi_test_challenge_action import SatoshiTestChallengeAction
from cobo_waas2.models.travel_rule_transaction_type import TravelRuleTransactionType
from typing import Optional, Set
from typing_extensions import Self


class CreateSatoshiTestChallengeRequest(BaseModel):
    """
    Request body for creating a Satoshi Test challenge. A single endpoint covers both the two-step flow (`PREPARE` then `SUBMIT`) and the one-shot flow (`SUBMIT` directly). Idempotent on `(chain_id, from_address)` per organization. 
    """  # noqa: E501
    action: SatoshiTestChallengeAction
    chain_id: StrictStr = Field(description="The chain ID of the counterparty address. See the operation description for supported chains.")
    from_address: StrictStr = Field(description="The counterparty (self-custody) wallet address that will transfer the micro-deposit.")
    transaction_id: StrictStr = Field(description="The Cobo transaction ID that this Satoshi Test is verifying for.")
    transaction_type: TravelRuleTransactionType
    challenge_id: Optional[StrictStr] = Field(default=None, description="The `challenge_id` returned by a previous `PREPARE` call. - When `action=SUBMIT`: if provided, activates that specific challenge by id (recommended when you cached the id client-side after `PREPARE`). If omitted, the server activates the latest matching challenge identified by `(chain_id, from_address)`. - When `action=PREPARE`: **must be omitted**. A new challenge is always generated; passing a `challenge_id` here will cause the request to be rejected. ")
    __properties: ClassVar[List[str]] = ["action", "chain_id", "from_address", "transaction_id", "transaction_type", "challenge_id"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CreateSatoshiTestChallengeRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CreateSatoshiTestChallengeRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "action": obj.get("action"),
            "chain_id": obj.get("chain_id"),
            "from_address": obj.get("from_address"),
            "transaction_id": obj.get("transaction_id"),
            "transaction_type": obj.get("transaction_type"),
            "challenge_id": obj.get("challenge_id")
        })
        return _obj


